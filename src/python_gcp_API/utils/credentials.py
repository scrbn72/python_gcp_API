# from google shell run gcloud auth application-default print-access-token
def use_default_credentials(credentials_path):
    """Use default Application Credentials."""
    print("Using default Google Application Credentials.")